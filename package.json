{
  "name": "tasks-api",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal in-memory tasks API (Node.js, zero dependencies).",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  }
}
